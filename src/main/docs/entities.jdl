entity Terminal {
	title String,
}

dto * with mapstruct

// Set service options to all except few
//service all with serviceImpl except Employee, Job, DataColumn
// Set an angular suffix
//angularSuffix * with spshl
